{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "npm run dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.2.0"
  },
  "dependencies": {
    "tailwind": "^4.0.0"
  }
}
