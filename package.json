{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "npm run dev -- -o",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.2.0"
  },
  "dependencies": {
    "@nuxt/kit": "npm:@nuxt/kit-edge@latest",
    "tailwind": "^4.0.0"
  }
}
